package user

import "log"

func CreateUser() error {
	log.Print("usually we process/verify data/auth here in manager and then send request to stores files")
	return nil
}

func UpdateUser() error {
	log.Print("usually we process/verify data/auth here in manager and then send request to stores files")
	return nil
}

func GetUser() error {
	log.Print("usually we process/verify data/auth here in manager and then send request to stores files")
	return nil
}

func DeleteUser() error {
	log.Print("usually we process/verify data/auth here in manager and then send request to stores files")
	return nil
}